.inbox
  .col-xs-2
  .col-xs-8
    h3 Inbox
    each message in messages
      .message
        h4.subject= message.subject
        h6.date= moment(message.date).fromNow()
        h5.body= message.body
        h4.from From: #{message.sender.username}
  .col-xs-2
